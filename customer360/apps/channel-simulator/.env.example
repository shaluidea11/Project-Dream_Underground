# Server
NODE_ENV=development
PORT=3002

# Shared secret — simulator validates incoming /send requests with this
SIMULATOR_SECRET=your-simulator-secret

# CRM callback URL — simulator posts delivery events here
CRM_CALLBACK_URL=http://localhost:3001/callbacks/delivery

# Sentry (optional — leave empty to disable)
SENTRY_DSN=
